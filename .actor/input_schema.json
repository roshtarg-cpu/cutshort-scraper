{
  "schemaVersion": 1,
  "title": "Cutshort Scraper Input",
  "type": "object",
  "description": "Configure the scraper parameters",
  "properties": {
    "startUrl": {
      "type": "string",
      "title": "Start URL",
      "description": "The Cutshort.io URL to scrape (e.g., https://cutshort.io/jobs/python-jobs)",
      "editor": "textfield",
      "example": "https://cutshort.io/jobs/python-jobs",
      "prefill": "https://cutshort.io/jobs"
    },
    "maxResults": {
      "type": "integer",
      "title": "Max Results",
      "description": "Maximum number of jobs to scrape",
      "editor": "number",
      "minimum": 1,
      "maximum": 1000,
      "default": 50,
      "prefill": 10
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy configuration to use for requests",
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      },
      "editor": "proxy"
    }
  },
  "required": ["startUrl"]
}
